# ------------------------------
# rest-api-modernized (FastAPI)
# ------------------------------
# Copy this file to `.env` and adjust values.
#
# Preview/dev defaults in this repo:
# - Backend port: 3002
# - Web client:   http://localhost:3003

# Service identity
APP_NAME=rest-api-modernized
APP_VERSION=0.1.0

# API routing
API_PREFIX=/api

# Server port (used by container/runtime; uvicorn CLI may override).
PORT=3002

# CORS: recommended to pass JSON list (pydantic-settings supports parsing it)
# Example: BACKEND_CORS_ORIGINS=["http://localhost:3003"]
BACKEND_CORS_ORIGINS=["http://localhost:3003"]

# Logging
LOG_LEVEL=INFO

# Database (async SQLAlchemy / asyncpg)
# Example:
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/rest_api_modernized
DATABASE_URL=

# OIDC / Keycloak-compatible auth (JWT validation)
# Example:
# OIDC_ISSUER_URL=https://keycloak.example.com/realms/<realm>
OIDC_ISSUER_URL=
# Expected audience for access tokens (often "account" or the API client id, depending on Keycloak mappers)
OIDC_AUDIENCE=
# Client id for extracting roles from resource_access[client_id].roles
OIDC_CLIENT_ID=
# Cache TTL for discovery/JWKS fetches
OIDC_CACHE_TTL_SECONDS=300

# Back-compat placeholders (deprecated)
KEYCLOAK_ISSUER_URL=
KEYCLOAK_AUDIENCE=
