# Service identity
APP_NAME=rest-api-modernized
APP_VERSION=0.1.0

# API routing
API_PREFIX=/api

# CORS: recommended to pass JSON list (pydantic-settings supports parsing it)
# Example: BACKEND_CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]
BACKEND_CORS_ORIGINS=[]

# Logging
LOG_LEVEL=INFO

# Database (async SQLAlchemy / asyncpg)
# Example:
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/rest_api_modernized
DATABASE_URL=

# Placeholders for later steps
KEYCLOAK_ISSUER_URL=
KEYCLOAK_AUDIENCE=
