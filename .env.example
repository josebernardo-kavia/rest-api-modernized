# Service identity
APP_NAME=rest-api-modernized
APP_VERSION=0.1.0

# API routing
API_PREFIX=/api

# CORS: recommended to pass JSON list (pydantic-settings supports parsing it)
# Example: BACKEND_CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]
BACKEND_CORS_ORIGINS=[]

# Logging
LOG_LEVEL=INFO

# Placeholders for later steps
DATABASE_URL=
KEYCLOAK_ISSUER_URL=
KEYCLOAK_AUDIENCE=
